import { AppRegistry } from 'react-native';
import App from './app/App';
// The i18n import initializes i18n service for the whole app
import { i18n } from '@selise-react/shared';

AppRegistry.registerComponent('main', () => App);
